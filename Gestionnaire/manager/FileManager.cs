using System.IO;
using Gestionnaire.model;

namespace Gestionnaire.manager
{
    //Class to manage file operations, like read, write (append), creation? , compare entry with a login (used for connection to the gestionnaire) 
    public class FileManager
    {
        
        public FileManager()
        {
        }
    }
}